# Core
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0

# AI / LangChain
langchain>=0.3.0
langchain-openai>=0.3.0
langgraph>=0.2.0

# Vector store: không có. ADR-013 chốt embedding nằm trong SQLite dưới dạng
# BLOB, xếp hạng bằng cosine của numpy — không dựng service vector riêng.
# numpy là dependency trực tiếp vì chính nó là "vector store": giải BLOB thành
# float32 và tính cosine trên tập đã lọc (`src/services/library/retriever.py`).
numpy>=2.0.0

# Database — ADR-011: SQLite cho local/test, Supabase PostgreSQL cho bản deploy.
# Cùng một repository layer qua SQLAlchemy Core; embedding là LargeBinary
# (BLOB ở SQLite, BYTEA ở Postgres).
sqlalchemy>=2.0.0
alembic>=1.14.0
psycopg2-binary>=2.9.0

# Dev tools
ruff==0.16.0             # ghim CHẶT: >= làm style format nhảy giữa máy dev và CI (xem PR #34, #35)
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0        # gate "Test coverage >= 60%" ở plan.md §9 — CI đo bằng cái này
httpx>=0.28.0
xmlschema>=3.4.0,<5.0.0  # validate generated .xosc against vendored ASAM OpenSCENARIO 1.0 XSD
