# Core
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0

# AI / LangChain
langchain>=0.3.0
langchain-openai>=0.3.0
langgraph>=0.2.0

# Vector store — Qdrant (ADR-003). Linh Đan cần ngay, xem plan.md §11.5
qdrant-client>=1.12.0

# Database — bỏ comment khi ADR-011 chốt xong (Công + Chi)
# sqlalchemy>=2.0.0
# alembic>=1.14.0
# psycopg2-binary>=2.9.0

# Dev tools
ruff>=0.8.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0        # gate "Test coverage >= 60%" ở plan.md §9 — CI đo bằng cái này
httpx>=0.28.0
